@model RPG.Models.CharacterModal.Character

<table class="table table-bordered">
    <thead>
        <tr>
            <th style="width:100%">Base attack</th>
        </tr>
    </thead>
    <tr>
        <th class="pop" data-container="body" data-toggle="popover" data-placement="bottom" data-content="@Html.Raw(@Model.GetBaseAttackCalculationString())">@Model.GetBaseAttackString()</th>
    </tr>
</table>